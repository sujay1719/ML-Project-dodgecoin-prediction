# -*- coding: utf-8 -*-
"""Copy of dodgecoin prediction.py

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/1u8HEZDAZ0limX6eZdh8lqcepHozI0h_n
"""

import numpy as np
import pandas as pd

from google.colab import files
files.upload()

df = pd.read_csv('DOGE-USD.csv')
df =df.set_index(pd.DatetimeIndex(df['Date'].values))
#show data
df

df = df[['Close']]
df

#Create a variable to store the number of days into the future that we want to predict
prediction_days = 1
#create a new coloumn called Prediction
df['Prediction'] = df[['Close']].shift(-prediction_days)
df

X = np.array(df.drop(['Prediction'], 1))
X = X[:len(df) -prediction_days -1]
print(X)

y = np.array(df['Prediction'])
#Get all of y values except the last n+1 rows
y = y[:-prediction_days - 1]
print(y)

from sklearn.model_selection import train_test_split
x_train, x_test, y_train, y_test = train_test_split(X, y, test_size = 0.4)

